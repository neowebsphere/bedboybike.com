# Development Dockerfile with live reload
FROM node:18-alpine

# Install browser-sync globally (more reliable than live-server)
RUN npm install -g browser-sync

# Set working directory
WORKDIR /app

# Copy browser-sync config file
COPY bs-config.js /app/bs-config.js

# Expose ports: 8080 for web, 8081 for UI
EXPOSE 8080 8081

# Start browser-sync using config file (with polling for Windows Docker volumes)
CMD ["browser-sync", "start", "--config", "bs-config.js"]

